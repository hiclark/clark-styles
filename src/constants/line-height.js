// @flow

import { css } from 'styled-components';

const COPY = css`
  line-height: 1.5;
`;

const TITLE = css`
  line-height: 1.25;
`;

const SOLID = css`
  line-height: 1;
`;

const LINE_HEIGHT = {
  COPY,
  TITLE,
  SOLID,
};

export default LINE_HEIGHT;
